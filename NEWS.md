# dipca 0.1.0

* Initial CRAN release.
* Implements DiPCA, DiCCA (classic and compact inner models), and DiPLS.
* Rcpp-backed component extraction for DiPCA and classic DiCCA.
* S3 methods for `predict()` and `residuals()` on fitted models.
* Integration with the `multivarious` projection framework.
